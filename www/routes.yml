/:
  controller: Main
  action: index

/dashboard:
  controller: Main
  action: dashboard
  security: false
  role: []


/contact:
  controller: Main
  action: contact

/login:
  controller: Security
  action: login
  security: false
  role : []

dash/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/{slug}:
  controller:  Main
  action: dashboard
  security: true
  role: []